running on linux Ubuntu 16.04:

sudo apt-get install python-virtualenv

run:
$virtualenv venv
$source venv/bin/activate
$pip install selenium

download geckodriver from https://github.com/mozilla/geckodriver/releases

$ wget geckodriver-v0.19.1-linux64.tar.gz (using linux 64 in my case)

untar it and move geckodriver into venv/bin


use your favorite text editor to modify testC.py


cp testC.py into venv

cd into venv

run testC.py

type deactivate

to exit out of venv

IN ADDITION, use must be running the server so the website would work